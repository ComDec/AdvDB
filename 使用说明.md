# RepCRec 使用说明

## 项目简介

RepCRec 是一个分布式数据库并发控制与恢复系统的Python实现，包含：
- ✅ 多版本并发控制（MVCC）
- ✅ 可串行化快照隔离（SSI）
- ✅ 可用副本算法
- ✅ 站点故障恢复

## 快速运行

### 方式1：运行测试文件

```bash
cd RepCRec
python main.py test_basic.txt
```

### 方式2：交互式运行

```bash
python main.py
```

然后输入命令，例如：
```
> begin(T1)
> W(T1, x1, 100)
> R(T1, x1)
> end(T1)
> dump()
> quit
```

### 方式3：运行所有测试

```bash
./run_all_tests.sh
```

## 支持的命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `begin(T1)` | 开始读写事务 | `begin(T1)` |
| `beginRO(T1)` | 开始只读事务 | `beginRO(T2)` |
| `R(T1, x1)` | 读取变量 | `R(T1, x1)` |
| `W(T1, x1, 100)` | 写入变量 | `W(T1, x1, 100)` |
| `end(T1)` | 结束事务 | `end(T1)` |
| `fail(1)` | 站点失败 | `fail(1)` |
| `recover(1)` | 站点恢复 | `recover(1)` |
| `dump()` | 查看所有数据 | `dump()` |
| `dump(1)` | 查看站点1数据 | `dump(1)` |
| `dump(x1)` | 查看x1在所有站点 | `dump(x1)` |

## 测试用例说明

| 文件 | 测试内容 |
|------|---------|
| `test_basic.txt` | 基础读写操作 |
| `test_ww_conflict.txt` | 写-写冲突检测 |
| `test_rw_conflict.txt` | 读-写冲突检测（SSI） |
| `test_site_failure.txt` | 站点故障和恢复 |
| `test_replicated.txt` | 复制变量管理 |
| `test_snapshot.txt` | MVCC快照读取 |
| `test_readonly.txt` | 只读事务 |
| `test_comprehensive.txt` | 综合测试 |

## 系统配置

### 站点配置
- 10个数据站点（站点1-10）
- 每个站点可以独立失败和恢复

### 变量配置
- 20个变量（x1-x20）
- 奇数变量（x1, x3, ...）：非复制，存储在特定站点
- 偶数变量（x2, x4, ...）：复制在所有站点
- 初始值：变量xi的初始值为10*i

### 变量分布规则
- x1 → 站点2
- x2 → 所有站点
- x3 → 站点4
- x4 → 所有站点
- x5 → 站点6
- ...以此类推

公式：非复制变量xi存储在站点 `1 + (i mod 10)`

## 示例：完整的事务流程

```
// 示例1：简单的读写
begin(T1)           // T1开始
W(T1, x1, 100)      // T1写入x1=100
R(T1, x1)           // T1读取x1，返回100（读己之写）
end(T1)             // T1提交
dump()              // 查看结果

// 示例2：并发冲突
begin(T1)           // T1开始
begin(T2)           // T2开始
W(T1, x1, 100)      // T1写x1
W(T2, x1, 200)      // T2也写x1
end(T1)             // T1先提交 → 成功
end(T2)             // T2后提交 → 失败（WW冲突）

// 示例3：站点故障
fail(1)             // 站点1失败
begin(T1)
R(T1, x2)           // x2是复制变量，可从其他站点读取
end(T1)             // 成功
recover(1)          // 站点1恢复
```

## 预期输出示例

### 成功提交
```
T1 begins
T1 writes x1: 100 (to local workspace)
T1 commits
```

### 写-写冲突
```
T1 begins
T2 begins
T1 writes x1: 100 (to local workspace)
T2 writes x1: 200 (to local workspace)
T1 commits
T2 aborts (WW conflict)
```

### 站点故障
```
Site 1 fails
T1 begins
T1 waits (site 1 is down)
```

## 文档说明

- **README.md**：完整的项目文档（英文），包含算法详解
- **QUICKSTART.md**：快速入门指南（英文）
- **PROJECT_STRUCTURE.md**：项目结构详解
- **PROJECT_SUMMARY.md**：项目总结
- **使用说明.md**：本文件（中文简要说明）

## 技术特性

### 核心算法
1. **MVCC（多版本并发控制）**
   - 每个变量维护版本历史
   - 事务读取开始时的快照

2. **SSI（可串行化快照隔离）**
   - 检测写-写冲突（First Committer Wins）
   - 检测读-写冲突（防止写偏斜）

3. **Available Copies（可用副本）**
   - 读操作：从任意可用站点读取
   - 写操作：写入所有可用站点

4. **故障恢复**
   - 非复制变量恢复后立即可用
   - 复制变量需要新提交才可用（陈旧标志）

## 环境要求

- Python 3.7 或更高版本
- 无需安装额外依赖包

## 常见问题

### Q1: 为什么我的事务被中止了？
**A:** 可能原因：
- WW冲突：另一个事务先提交了相同变量
- RW冲突：违反了可串行化规则
- 站点故障：需要的站点不可用

### Q2: 如何创建自己的测试？
**A:** 创建一个`.txt`文件，每行一个命令，然后运行：
```bash
python main.py 你的测试文件.txt
```

### Q3: 注释怎么写？
**A:** 使用`//`开头：
```
// 这是注释
begin(T1)  // 行内注释也可以
```

## 项目统计

- 代码量：约900行
- 测试用例：8个
- 文档：5份
- 开发时间：可在10小时内完成
- 外部依赖：0个

## 获取帮助

查看以下文档获取更多信息：
1. **快速入门**：阅读 `QUICKSTART.md`
2. **详细文档**：阅读 `README.md`
3. **项目结构**：阅读 `PROJECT_STRUCTURE.md`
4. **运行测试**：查看 `test_*.txt` 文件

## 联系方式

如有问题，请查阅项目文档或参考测试用例。

---

**祝您使用愉快！**
