# RepCRec 快速入门指南

## 5分钟快速开始

### 1. 运行第一个测试

```bash
cd RepCRec
python main.py test_basic.txt
```

### 2. 交互式使用

```bash
python main.py
```

然后输入命令：

```
> begin(T1)
> W(T1, x1, 100)
> R(T1, x1)
> end(T1)
> dump()
> quit
```

### 3. 创建自己的测试文件

创建文件 `my_test.txt`：

```
// 我的第一个测试
begin(T1)
W(T1, x1, 200)
W(T1, x2, 300)
end(T1)
dump()
```

运行：
```bash
python main.py my_test.txt
```

## 常见命令示例

### 基础事务操作

```
begin(T1)           # 开始事务T1
W(T1, x1, 100)      # T1写入x1=100
R(T1, x2)           # T1读取x2
end(T1)             # T1提交
```

### 只读事务

```
beginRO(T2)         # 开始只读事务T2
R(T2, x1)           # 读取x1
R(T2, x2)           # 读取x2
end(T2)             # 提交（只读事务总是成功）
```

### 站点故障模拟

```
fail(1)             # 站点1失败
recover(1)          # 站点1恢复
```

### 查看数据

```
dump()              # 查看所有站点的所有数据
dump(1)             # 查看站点1的数据
dump(x1)            # 查看所有站点上x1的值
```

## 预期行为示例

### 示例1：写-写冲突（First Committer Wins）

```
begin(T1)
begin(T2)
W(T1, x1, 100)      # T1写x1
W(T2, x1, 200)      # T2也写x1
end(T1)             # T1先提交 -> 成功
end(T2)             # T2后提交 -> 失败（WW冲突）
```

输出：`T2 aborts (WW conflict)`

### 示例2：快照隔离

```
begin(T1)           # T1开始（时间戳=1）
begin(T2)           # T2开始（时间戳=2）
W(T1, x1, 100)
end(T1)             # T1提交
R(T2, x1)           # T2读取x1 -> 看到旧值10（T2的快照）
end(T2)
```

T2读取的是它开始时的快照，而不是T1提交后的新值。

### 示例3：站点恢复后的陈旧副本

```
fail(1)             # 站点1失败
W(T1, x2, 202)      # x2是复制变量，写入其他站点
end(T1)
recover(1)          # 站点1恢复
R(T2, x2)           # 可以读取（已从其他站点获得新值）
```

## 变量分布规则

- **x1, x3, x5, ..., x19**（奇数）：非复制
  - x1 在站点2
  - x3 在站点4
  - x5 在站点6
  - 规则：站点 = 1 + (变量索引 mod 10)

- **x2, x4, x6, ..., x20**（偶数）：复制在所有10个站点

## 调试技巧

1. **频繁使用 dump()**：在操作后添加 `dump()` 查看数据变化
2. **观察提交/中止消息**：系统会打印每个事务的结果
3. **测试站点故障**：fail和recover是测试系统鲁棒性的好方法
4. **注释测试**：使用 `//` 添加注释说明测试意图

## 更多测试用例

查看以下文件了解更复杂的场景：

- `test_ww_conflict.txt` - WW冲突
- `test_rw_conflict.txt` - RW冲突（SSI）
- `test_site_failure.txt` - 站点故障处理
- `test_replicated.txt` - 复制变量
- `test_snapshot.txt` - MVCC快照
- `test_comprehensive.txt` - 综合测试

## 常见问题

### Q: 为什么我的事务被中止了？

A: 可能的原因：
1. **WW冲突**：另一个事务先提交了相同变量的写入
2. **RW冲突**：违反了可串行化快照隔离规则
3. **站点故障**：事务读取的非复制变量所在站点失败

### Q: 如何测试并发？

A: 同时开始多个事务，交替执行它们的操作：
```
begin(T1)
begin(T2)
W(T1, x1, 100)
W(T2, x2, 200)
end(T1)
end(T2)
```

### Q: 站点恢复后为什么有些变量不可读？

A: 复制变量在恢复后被标记为"陈旧"，需要有新的提交写入才能再次可读。非复制变量立即可读。

## 下一步

- 阅读完整的 `README.md` 了解算法细节
- 修改现有测试用例进行实验
- 创建自己的复杂测试场景

祝测试愉快！
